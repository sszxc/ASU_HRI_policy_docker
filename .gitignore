# colcon build output (src is the only part that's source)
build/
install/
log/

__pycache__/
*.pyc

# large caches/local data, not part of the project backup
.cache/
.local/

# Claude Code local worktree state, not part of the project source
.claude/worktrees/
