<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>policy_runner</name>
  <version>0.0.1</version>
  <description>
    Subscribes camera images + joint_states over ROS2 (rclpy) and serves a
    browser page (web_monitor) showing which streams are live, adapted from
    trajectory_recorder's capture console minus the recording pipeline.
    This is the subscriber half of the ACT inference node planned in the
    repo README ("Next phase"), built and verified here first.
  </description>
  <maintainer email="im.zhangxc@gmail.com">asu</maintainer>
  <license>TODO</license>

  <depend>rclpy</depend>
  <depend>sensor_msgs</depend>
  <depend>ament_index_python</depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
