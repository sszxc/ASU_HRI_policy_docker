This is an agile development project; whenever you're asked to make a commit, just commit directly to the `master` branch.

Start the docker using `./start_docker.sh` and access it using `./exec-docker.sh`
